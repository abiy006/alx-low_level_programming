#include "main.h"

/**
 * _abs - accept the value and return the absolute value
 *
 * @t: the character
 *
 * Return: absoulte value
 */

int _abs(int t)
{
	if (t >= 0)
	{
		return (t);
	}
	else
	{
		return (-t);
	}
}

