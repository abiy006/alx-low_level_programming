#include "abiy.h"
#include <stdio.h>
/**
 * is_palindrome -return is string is a palindrome
 * @s: string ...
 * Return: integer ...
 **/

int is_palindrome(char *s)
{
	int i;

	i = 0;
	if (*s == i)
	{
		return (0);
	}
	else
	{
		return (1);
	}
}
