#include "abiy.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * main -  print name
 *@argi: int.
 *@argj: char.
 * Return: Always 0.
 */
int main(int argi, char *argj[])
{
	int a;

	for (a = 0 ; a < argi ; a++)
	{
		printf("%s\n", argj[i]);
	}
	return (0);
}
